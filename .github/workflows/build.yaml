on:
  push:
    branches: 
      - 'release-6.2'
         
env:
  RELEASE_START: 1186 # 1150, as this is latest build by GitLab CI
  CCACHE_DIR: $RUNNER_TEMP/ccache

jobs:
  build:
    runs-on: ubuntu-latest
    permissions: write-all
    container: 
      image: gustavokch/rock64build:jammy
      credentials:
        username: gustavokch
        password: dckr_pat_HzZm-FaY8cD_h88RZoEfhWTyC9A
    steps:
    - name: Checkout
      uses: actions/checkout@v3
    - name: Install Docker binary
      run: |
        apt-get -y update
        apt-get -y install docker.io
    - name: Set up QEMU
      uses: docker/setup-qemu-action@v2
    - name: Reset ccache statistics
      run: ccache -M 0 -F 0
    - name: Build package
      run: |
        export RELEASE=$(($RELEASE_START+$GITHUB_RUN_NUMBER))
        ./dev-make kernel-package
    - name: Archive production artifacts
      uses: actions/upload-artifact@v3
      with:
          name: .deb output
          path: /__w/linux-mainline-kernel/linux-mainline-kernel/*.deb
    - name: Archive production artifacts2
      uses: actions/upload-artifact@v3
      with:
          name: debian
          path: /*.deb
          
